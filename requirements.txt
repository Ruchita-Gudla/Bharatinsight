# BharatInsight — full-stack requirements (Phases 0–5)
# Phase 0 itself needs only: pandas, sqlalchemy, psycopg2-binary, python-dotenv
# The rest is declared now so later phases run without revisiting installs.
# NOTE (Phase 5): pin exact versions (pip freeze) before deploying to
# Streamlit Community Cloud — unpinned builds can resolve differently there.

# --- Phase 0: database layer ----------------------------------------------
pandas>=2.0
sqlalchemy>=2.0
psycopg2-binary>=2.9.6
python-dotenv>=1.0

# --- Phase 3: analytics & ML ----------------------------------------------
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3
xgboost>=2.0
prophet>=1.1.4        # Windows: needs "Microsoft C++ Build Tools", or use `conda install -c conda-forge prophet`
mlxtend>=0.22

# --- Phase 3/5: visualization ----------------------------------------------
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# --- Phase 3: notebooks ------------------------------------------------------
jupyter>=1.0
ipykernel>=6.25

# --- Phase 5: app layer ------------------------------------------------------
streamlit>=1.28
